# rds-init points log_error here; without it the server's own errors go to
# syslog, which nothing collects off a guest with no SSH. A tmpfiles entry
# rather than a baked directory: the image build cannot chown.
d /var/log/mysql 0750 mysql mysql -
