# Per-boot diagnostics, on the boot volume so a snapshot stays data-only.
# A tmpfiles entry rather than a baked directory: the image build cannot chown.
d /var/log/postgresql 0755 postgres postgres -
